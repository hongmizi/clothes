<p>
  <%=link_to product.title, edit_admin_product_path(product)%>
</p>
<% product.listings.each do |list|%>
<p>
  <%= "#{list.color} #{list.size} 库存:#{list.stock} 价格:#{list.price}"%>
</p>
<% end %>
<p>
  <%= link_to "增加新类别","" %>
</p>
